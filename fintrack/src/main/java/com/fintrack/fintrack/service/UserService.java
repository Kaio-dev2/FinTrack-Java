package com.fintrack.fintrack.service;

import org.springframework.stereotype.Service;

// @Service avisa o Spring: "essa classe é um Service"
// O Spring passa a gerenciá-la automaticamente — isso se chama Injeção de Dependência
@Service
public class UserService {

    // aqui vai a lógica de negócio do sistema
    // ex: validar senha, verificar se email já existe, etc

}